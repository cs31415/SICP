(define (make-node value child-nodes)
  (cons value child-nodes))

(define (get-value node)
  (car node))

(define (get-children node)
  (cadr node))

(define (make-graph node-list)
  node-list)

(define (add-edge graph val1 val2)
  (


